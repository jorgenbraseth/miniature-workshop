# Miniature Workshop Frontend Environment Variables

# Google OAuth Client ID (required for authentication)
# Get this from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a new project or select existing project
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add your domain to authorized origins
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# API Base URL (automatically set by GitHub Actions during deployment)
# For local development, use your local backend or deployed backend URL
VITE_API_BASE_URL=https://your-api-gateway-url.amazonaws.com

# Development settings
# VITE_API_BASE_URL=http://localhost:3000
